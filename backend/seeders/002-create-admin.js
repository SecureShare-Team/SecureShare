'use strict';

const bcrypt = require('bcryptjs');

module.exports = {
  up: async (queryInterface) => {
    // TODO: 초기 관리자 계정 만들기
  },

  down: async (queryInterface) => {
    // TODO: 롤를백 구현
  }
};
