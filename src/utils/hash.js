/**
 * utils/hash.js — SHA-256 해시 계산 (파일 무결성 검증용)
 *
 * 파일 업로드 시 원본 파일(암호화 전)의 해시를 계산해 files 테이블에 저장
 * 추후 다운로드 시 클라이언트가 수신 파일의 해시와 비교해 무결성 검증 가능
 *
 * 함수 목록:
 *
 *   computeSHA256(buffer)
 *     → hex string (64자리 16진수 문자열)
 *     → Node.js 내장 crypto 모듈 사용 (외부 의존성 없음)
 *     → 동일 입력 → 항상 동일 출력 (결정론적)
 *     → 다른 입력 → 다른 출력 (충돌 저항성)
 *
 * 담당: A
 */
